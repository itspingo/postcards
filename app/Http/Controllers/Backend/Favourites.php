<?php

namespace App\Http\Controllers\Backend;
use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

use App\Models\favourites_model;


/* ******************************************************
    This file is generated by SNOOBIX on May 16, 2024
    Please visit snoobix.com for more details
****************************************************** */


class Favourites extends Controller
{
    public function __construct()
    {
        //if (!Auth::check()) return 'access_denied';
    }
   
    public function index()
    { 
        $data['module_title'] = 'Favourites';
		$data['rows'] = favourites_model::orderBy('id', 'desc')->get();
		return view("backend/favourites_list", $data);
	
    }


    public function create()
    {
        $data['module_title'] = 'Favourites';
       //$data['row'] = [];
       $data['submit_path'] = 'store';
       $data['module_name'] = 'Favourites';
       return view("backend/favourites_addedit", $data);       
    }

   
    public function store(Request $request)
    {
         $data['module_title'] = 'Favourites';
        /* $request->validate([
            [[validateFieldsArray]]
        ]); */

        /* $record = [
			[[fieldsArray]]
		];
		$iscreated = favourites_model::create($record); */

        $record = $request->all();
        
        
        $savedid = favourites_model::create($record)->id;
		if($savedid){
			 //return view("backend/favourites_addedit")->with('flash_success', "Record is saved successfuly");
             return redirect('backend/favourites/create')->with('flash_success', "Record is saved successfuly");
		}else{
			$data['row'] = $request->all();
			return view("backend/favourites_addedit", $data)->with('flash_failure', "Record is NOT saved successfuly");
		}
    }

    
    public function show()
    {
        //
    }

    
    public function edit($id)
    {
         $data['module_title'] = 'Favourites';
        $data['submit_path'] = 'update';
        $recCond = [
			'id' => str2id($id)
		];
		$data['row'] = favourites_model::where($recCond)->first();
		return view("backend/favourites_addedit", $data);
    }

   
    public function update(Request $request)
    {
		 $data['module_title'] = 'Favourites';
        $id = $request->post('id');
        $id = str2id($id);
        $vfavourites_model = favourites_model::find($id);
        
         $record = $request->all();
         
        
        $isaved = $vfavourites_model->fill($record)->save();  //this will save record with all fillable items

        if($isaved){
			//return view("backend/favourites_addedit")->with('flash_success', "Record is updated successfuly");
            return redirect('backend/favourites')->with('flash_success', "Record is updated successfuly");
		}else{

			//$data['postData'] = $record;
             $data['submit_path'] = 'update';
             $recCond = [
                'id' => $id,
            ];
            $data['row'] = favourites_model::where($recCond)->first();
			return view("backend/favourites_addedit", $data)->with('flash_failure', "Record is NOT updated successfuly");
		}		
    }

   
    public function destroy(Request $request)
    {
        $id = $request->post('delid');
        $id = str2id($id);
        $vfavourites_model = favourites_model::find($id);
        $isdeleted = $vfavourites_model->delete();
        if($isdeleted ){
            return redirect()->route('backend.favourites')->with('flash_success','Record is deleted successfuly');
        }else{
            return redirect()->route('backend.favourites')->with('flash_failure','Record is not deleted, try again later');
        }
    }
}
