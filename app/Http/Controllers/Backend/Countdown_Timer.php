<?php

namespace App\Http\Controllers\Backend;
use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

use App\Models\countdown_timer_model;


/* ******************************************************
    This file is generated by SNOOBIX on Jul 30, 2024
    Please visit snoobix.com for more details
****************************************************** */


class Countdown_Timer extends Controller
{
    public function __construct()
    {
        //if (!Auth::check()) return 'access_denied';
    }
   
    public function index()
    { 
        $data['module_title'] = 'Countdown Timer';
		$data['rows'] = countdown_timer_model::orderBy('id', 'desc')->get();
		return view("backend/countdown_timer_list", $data);
	
    }


    public function create()
    {
        $data['module_title'] = 'Countdown Timer';
       //$data['row'] = [];
       $data['submit_path'] = 'store';
       $data['module_name'] = 'Countdown Timer';
       return view("backend/countdown_timer_addedit", $data);       
    }

   
    public function store(Request $request)
    {
         $data['module_title'] = 'Countdown Timer';
        
        $record = $request->all();
        
        
        $savedid = countdown_timer_model::create($record)->id;
		if($savedid){
			 //return view("backend/countdown_timer_addedit")->with('flash_success', "Record is saved successfuly");
             return redirect('backend/countdown_timer/create')->with('flash_success', "Record is saved successfuly");
		}else{
			$data['row'] = $request->all();
			return view("backend/countdown_timer_addedit", $data)->with('flash_failure', "Record is NOT saved successfuly");
		}
    }

    
    public function show()
    {
        //
    }

    
    public function edit($id)
    {
         $data['module_title'] = 'Countdown Timer';
        $data['submit_path'] = 'update';
        $recCond = [
			'id' => str2id($id)
		];
		$data['row'] = countdown_timer_model::where($recCond)->first();
		return view("backend/countdown_timer_addedit", $data);
    }

   
    public function update(Request $request)
    {
		 $data['module_title'] = 'Countdown Timer';
        $id = $request->post('id');
        $id = str2id($id);
        $vcountdown_timer_model = countdown_timer_model::find($id);
        
         $record = $request->all();
         
        
        $isaved = $vcountdown_timer_model->fill($record)->save();  //this will save record with all fillable items

        if($isaved){
			//return view("backend/countdown_timer_addedit")->with('flash_success', "Record is updated successfuly");
            return redirect('backend/countdown_timer')->with('flash_success', "Record is updated successfuly");
		}else{

			//$data['postData'] = $record;
             $data['submit_path'] = 'update';
             $recCond = [
                'id' => $id,
            ];
            $data['row'] = countdown_timer_model::where($recCond)->first();
			return view("backend/countdown_timer_addedit", $data)->with('flash_failure', "Record is NOT updated successfuly");
		}		
    }

   
    public function destroy(Request $request)
    {
        $id = $request->post('delid');
        $id = str2id($id);
        $vcountdown_timer_model = countdown_timer_model::find($id);
        $isdeleted = $vcountdown_timer_model->delete();
        if($isdeleted ){
            return redirect()->route('backend.countdown_timer')->with('flash_success','Record is deleted successfuly');
        }else{
            return redirect()->route('backend.countdown_timer')->with('flash_failure','Record is not deleted, try again later');
        }
    }
}
