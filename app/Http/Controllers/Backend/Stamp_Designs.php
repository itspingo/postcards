<?php

namespace App\Http\Controllers\Backend;
use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

use App\Models\stamp_designs_model;


/* ******************************************************
    This file is generated by SNOOBIX on May 16, 2024
    Please visit snoobix.com for more details
****************************************************** */


class Stamp_Designs extends Controller
{
    public function __construct()
    {
        //if (!Auth::check()) return 'access_denied';
    }
   
    public function index()
    { 
        $data['module_title'] = 'Stamp Designs';
		$data['rows'] = stamp_designs_model::orderBy('id', 'desc')->get();
		return view("backend/stamp_designs_list", $data);
	
    }


    public function create()
    {
        $data['module_title'] = 'Stamp Designs';
       //$data['row'] = [];
       $data['submit_path'] = 'store';
       $data['module_name'] = 'Stamp Designs';
       return view("backend/stamp_designs_addedit", $data);       
    }

   
    public function store(Request $request)
    {
         $data['module_title'] = 'Stamp Designs';
        /* $request->validate([
            [[validateFieldsArray]]
        ]); */

        /* $record = [
			[[fieldsArray]]
		];
		$iscreated = stamp_designs_model::create($record); */

        $record = $request->all();
        if ($request->hasFile('main_image')) {$imgFile = $request->file('main_image')->store('public');$record['main_image'] = $imgFile;}
        if ($request->hasFile('more_images')) {$imagePaths = [];foreach ($request->file('more_images') as $image) {$imagePath = $image->store('public');$imagePaths[] = $imagePath;}$record['more_images'] = implode(',', $imagePaths);}
        $savedid = stamp_designs_model::create($record)->id;
		if($savedid){
			 //return view("backend/stamp_designs_addedit")->with('flash_success', "Record is saved successfuly");
             return redirect('backend/stamp_designs/create')->with('flash_success', "Record is saved successfuly");
		}else{
			$data['row'] = $request->all();
			return view("backend/stamp_designs_addedit", $data)->with('flash_failure', "Record is NOT saved successfuly");
		}
    }

    
    public function show()
    {
        //
    }

    
    public function edit($id)
    {
         $data['module_title'] = 'Stamp Designs';
        $data['submit_path'] = 'update';
        $recCond = [
			'id' => str2id($id)
		];
		$data['row'] = stamp_designs_model::where($recCond)->first();
		return view("backend/stamp_designs_addedit", $data);
    }

   
    public function update(Request $request)
    {
		 $data['module_title'] = 'Stamp Designs';
        $id = $request->post('id');
        $id = str2id($id);
        $vstamp_designs_model = stamp_designs_model::find($id);
        
         $record = $request->all();
         if ($request->hasFile('main_image')) {$imgFile = $request->file('main_image')->store('public');$record['main_image'] = $imgFile;}
        if ($request->hasFile('more_images')) {$imagePaths = [];foreach ($request->file('more_images') as $image) {$imagePath = $image->store('public');$imagePaths[] = $imagePath;}$record['more_images'] = implode(',', $imagePaths);}
        $isaved = $vstamp_designs_model->fill($record)->save();  //this will save record with all fillable items

        if($isaved){
			//return view("backend/stamp_designs_addedit")->with('flash_success', "Record is updated successfuly");
            return redirect('backend/stamp_designs')->with('flash_success', "Record is updated successfuly");
		}else{

			//$data['postData'] = $record;
             $data['submit_path'] = 'update';
             $recCond = [
                'id' => $id,
            ];
            $data['row'] = stamp_designs_model::where($recCond)->first();
			return view("backend/stamp_designs_addedit", $data)->with('flash_failure', "Record is NOT updated successfuly");
		}		
    }

   
    public function destroy(Request $request)
    {
        $id = $request->post('delid');
        $id = str2id($id);
        $vstamp_designs_model = stamp_designs_model::find($id);
        $isdeleted = $vstamp_designs_model->delete();
        if($isdeleted ){
            return redirect()->route('backend.stamp_designs')->with('flash_success','Record is deleted successfuly');
        }else{
            return redirect()->route('backend.stamp_designs')->with('flash_failure','Record is not deleted, try again later');
        }
    }
}
