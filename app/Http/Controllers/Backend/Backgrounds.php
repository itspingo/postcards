<?php

namespace App\Http\Controllers\Backend;
use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

use App\Models\backgrounds_model;


/* ******************************************************
    This file is generated by SNOOBIX on May 24, 2024
    Please visit snoobix.com for more details
****************************************************** */


class Backgrounds extends Controller
{
    public function __construct()
    {
        //if (!Auth::check()) return 'access_denied';
    }
   
    public function index()
    { 
        $data['module_title'] = 'Backgrounds';
		$data['rows'] = backgrounds_model::orderBy('id', 'desc')->get();
		return view("backend/backgrounds_list", $data);
	
    }


    public function create()
    {
        $data['module_title'] = 'Backgrounds';
       //$data['row'] = [];
       $data['submit_path'] = 'store';
       $data['module_name'] = 'Backgrounds';
       return view("backend/backgrounds_addedit", $data);       
    }

   
    public function store(Request $request)
    {
         $data['module_title'] = 'Backgrounds';
        /* $request->validate([
            [[validateFieldsArray]]
        ]); */

        /* $record = [
			[[fieldsArray]]
		];
		$iscreated = backgrounds_model::create($record); */

        $record = $request->all();
        if ($request->hasFile('main_image')) {$imgFile = $request->file('main_image')->store('public');$record['main_image'] = $imgFile;}
        if ($request->hasFile('more_images')) {
            $imagePaths = [];
            foreach ($request->file('more_images') as $image) {
                $imagePath = $image->store('public');
                $imagePaths[] = $imagePath;
            }
            $record['more_images'] = implode(',', $imagePaths);
        }
        $savedid = backgrounds_model::create($record)->id;
		if($savedid){
			 //return view("backend/backgrounds_addedit")->with('flash_success', "Record is saved successfuly");
             return redirect('backend/backgrounds/create')->with('flash_success', "Record is saved successfuly");
		}else{
			$data['row'] = $request->all();
			return view("backend/backgrounds_addedit", $data)->with('flash_failure', "Record is NOT saved successfuly");
		}
    }

    
    public function show()
    {
        //
    }

    
    public function edit($id)
    {
         $data['module_title'] = 'Backgrounds';
        $data['submit_path'] = 'update';
        $recCond = [
			'id' => str2id($id)
		];
		$data['row'] = backgrounds_model::where($recCond)->first();
		return view("backend/backgrounds_addedit", $data);
    }

   
    public function update(Request $request)
    {
		 $data['module_title'] = 'Backgrounds';
        $id = $request->post('id');
        $id = str2id($id);
        $vbackgrounds_model = backgrounds_model::find($id);
        
         $record = $request->all();
         if ($request->hasFile('main_image')) {$imgFile = $request->file('main_image')->store('public');$record['main_image'] = $imgFile;}
        if ($request->hasFile('more_images')) {$imagePaths = [];foreach ($request->file('more_images') as $image) {$imagePath = $image->store('public');$imagePaths[] = $imagePath;}$record['more_images'] = implode(',', $imagePaths);}
        $isaved = $vbackgrounds_model->fill($record)->save();  //this will save record with all fillable items

        if($isaved){
			//return view("backend/backgrounds_addedit")->with('flash_success', "Record is updated successfuly");
            return redirect('backend/backgrounds')->with('flash_success', "Record is updated successfuly");
		}else{

			//$data['postData'] = $record;
             $data['submit_path'] = 'update';
             $recCond = [
                'id' => $id,
            ];
            $data['row'] = backgrounds_model::where($recCond)->first();
			return view("backend/backgrounds_addedit", $data)->with('flash_failure', "Record is NOT updated successfuly");
		}		
    }

   
    public function destroy(Request $request)
    {
        $id = $request->post('delid');
        $id = str2id($id);
        $vbackgrounds_model = backgrounds_model::find($id);
        $isdeleted = $vbackgrounds_model->delete();
        if($isdeleted ){
            return redirect()->route('backend.backgrounds')->with('flash_success','Record is deleted successfuly');
        }else{
            return redirect()->route('backend.backgrounds')->with('flash_failure','Record is not deleted, try again later');
        }
    }
}
