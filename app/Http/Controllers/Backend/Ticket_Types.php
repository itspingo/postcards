<?php

namespace App\Http\Controllers\Backend;
use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

use App\Models\ticket_types_model;


/* ******************************************************
    This file is generated by SNOOBIX on Aug 03, 2024
    Please visit snoobix.com for more details
****************************************************** */


class Ticket_Types extends Controller
{
    public function __construct()
    {
        //if (!Auth::check()) return 'access_denied';
    }
   
    public function index()
    { 
        $data['module_title'] = 'Ticket Types';
		$data['rows'] = ticket_types_model::orderBy('id', 'desc')->get();
		return view("backend/ticket_types_list", $data);
	
    }


    public function create()
    {
        $data['module_title'] = 'Ticket Types';
       //$data['row'] = [];
       $data['submit_path'] = 'store';
       $data['module_name'] = 'Ticket Types';
       return view("backend/ticket_types_addedit", $data);       
    }

   
    public function store(Request $request)
    {
         $data['module_title'] = 'Ticket Types';
        /* $request->validate([
            [[validateFieldsArray]]
        ]); */

        /* $record = [
			[[fieldsArray]]
		];
		$iscreated = ticket_types_model::create($record); */

        $record = $request->all();
        if ($request->hasFile('ticket_image')) {
$imgFile = $request->file('ticket_image')->store('public');
$record['ticket_image'] = $imgFile;
}

        
        $savedid = ticket_types_model::create($record)->id;
		if($savedid){
			 //return view("backend/ticket_types_addedit")->with('flash_success', "Record is saved successfuly");
             return redirect('backend/ticket_types/create')->with('flash_success', "Record is saved successfuly");
		}else{
			$data['row'] = $request->all();
			return view("backend/ticket_types_addedit", $data)->with('flash_failure', "Record is NOT saved successfuly");
		}
    }

    
    public function show()
    {
        //
    }

    
    public function edit($id)
    {
         $data['module_title'] = 'Ticket Types';
        $data['submit_path'] = 'update';
        $recCond = [
			'id' => str2id($id)
		];
		$data['row'] = ticket_types_model::where($recCond)->first();
		return view("backend/ticket_types_addedit", $data);
    }

   
    public function update(Request $request)
    {
		 $data['module_title'] = 'Ticket Types';
        $id = $request->post('id');
        $id = str2id($id);
        $vticket_types_model = ticket_types_model::find($id);
        
         $record = $request->all();
         if ($request->hasFile('ticket_image')) {
$imgFile = $request->file('ticket_image')->store('public');
$record['ticket_image'] = $imgFile;
}

        
        $isaved = $vticket_types_model->fill($record)->save();  //this will save record with all fillable items

        if($isaved){
			//return view("backend/ticket_types_addedit")->with('flash_success', "Record is updated successfuly");
            return redirect('backend/ticket_types')->with('flash_success', "Record is updated successfuly");
		}else{

			//$data['postData'] = $record;
             $data['submit_path'] = 'update';
             $recCond = [
                'id' => $id,
            ];
            $data['row'] = ticket_types_model::where($recCond)->first();
			return view("backend/ticket_types_addedit", $data)->with('flash_failure', "Record is NOT updated successfuly");
		}		
    }

   
    public function destroy(Request $request)
    {
        $id = $request->post('delid');
        $id = str2id($id);
        $vticket_types_model = ticket_types_model::find($id);
        $isdeleted = $vticket_types_model->delete();
        if($isdeleted ){
            return redirect()->route('backend.ticket_types')->with('flash_success','Record is deleted successfuly');
        }else{
            return redirect()->route('backend.ticket_types')->with('flash_failure','Record is not deleted, try again later');
        }
    }
}
