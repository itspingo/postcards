<?php

namespace App\Http\Controllers\Backend;
use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

use App\Models\card_categories_model;


/* ******************************************************
    This file is generated by SNOOBIX on May 16, 2024
    Please visit snoobix.com for more details
****************************************************** */


class Card_Categories extends Controller
{
    public function __construct()
    {
        //if (!Auth::check()) return 'access_denied';
    }
   
    public function index()
    { 
        $data['module_title'] = 'Card Categories';
		$data['rows'] = card_categories_model::orderBy('id', 'desc')->get();
		return view("backend/card_categories_list", $data);
	
    }


    public function create()
    {
        $data['module_title'] = 'Card Categories';
       //$data['row'] = [];
       $data['submit_path'] = 'store';
       $data['module_name'] = 'Card Categories';
       return view("backend/card_categories_addedit", $data);       
    }

   
    public function store(Request $request)
    {
         $data['module_title'] = 'Card Categories';
        /* $request->validate([
            [[validateFieldsArray]]
        ]); */

        /* $record = [
			[[fieldsArray]]
		];
		$iscreated = card_categories_model::create($record); */

        $record = $request->all();
        if ($request->hasFile('cat_image')) {$imgFile = $request->file('cat_image')->store('public');$record['cat_image'] = $imgFile;}
        
        $savedid = card_categories_model::create($record)->id;
		if($savedid){
			 //return view("backend/card_categories_addedit")->with('flash_success', "Record is saved successfuly");
             return redirect('backend/card_categories/create')->with('flash_success', "Record is saved successfuly");
		}else{
			$data['row'] = $request->all();
			return view("backend/card_categories_addedit", $data)->with('flash_failure', "Record is NOT saved successfuly");
		}
    }

    
    public function show()
    {
        //
    }

    
    public function edit($id)
    {
         $data['module_title'] = 'Card Categories';
        $data['submit_path'] = 'update';
        $recCond = [
			'id' => str2id($id)
		];
		$data['row'] = card_categories_model::where($recCond)->first();
		return view("backend/card_categories_addedit", $data);
    }

   
    public function update(Request $request)
    {
		 $data['module_title'] = 'Card Categories';
        $id = $request->post('id');
        $id = str2id($id);
        $vcard_categories_model = card_categories_model::find($id);
        
         $record = $request->all();
         if ($request->hasFile('cat_image')) {$imgFile = $request->file('cat_image')->store('public');$record['cat_image'] = $imgFile;}
        
        $isaved = $vcard_categories_model->fill($record)->save();  //this will save record with all fillable items

        if($isaved){
			//return view("backend/card_categories_addedit")->with('flash_success', "Record is updated successfuly");
            return redirect('backend/card_categories')->with('flash_success', "Record is updated successfuly");
		}else{

			//$data['postData'] = $record;
             $data['submit_path'] = 'update';
             $recCond = [
                'id' => $id,
            ];
            $data['row'] = card_categories_model::where($recCond)->first();
			return view("backend/card_categories_addedit", $data)->with('flash_failure', "Record is NOT updated successfuly");
		}		
    }

   
    public function destroy(Request $request)
    {
        $id = $request->post('delid');
        $id = str2id($id);
        $vcard_categories_model = card_categories_model::find($id);
        $isdeleted = $vcard_categories_model->delete();
        if($isdeleted ){
            return redirect()->route('backend.card_categories')->with('flash_success','Record is deleted successfuly');
        }else{
            return redirect()->route('backend.card_categories')->with('flash_failure','Record is not deleted, try again later');
        }
    }
}
